#include<iostream>
using namespace std;
int main(){
    int a;
    int b;
    cout<<"Enter Your Valude A :";cin>>a;
    cout<<"Enter your Valude B :";cin>>b;
    if(a>b){
        cout<<"The value A is BIG  "<<endl;
    }else if(a<b){
        cout<<"The value B is BIG "<<endl;
    }else{
        cout<<"No Data !!"<<endl;
    }

    return 0;
}
//                    CONTROL STATEMENT LESSON !!
// Note ចំណុចដែលត្រូវចងចាំ!!
// If It's show the ture valude 
// ==> វាបង្ហាញ ឬ អនុវត្តតែលខណ្ឌដែលពិតតែប៉ុណ្ណោះ ទើបវាបង្ហាញទិន្ន័យ
// else It's show the false valude when the if is false!!
// ==> វាបង្ហាញ ឬ​ អនុវត្តនៅពេលដែលលខណ្ឌ if មិនពិត ទើបវាបង្ហាញទិន្ន័យ
// else if It's show the false valude but before show the valude it cheak the statement first when the statement was cheak and ture that the statement it's will Apply the statement or show the valude or Data!!
// វាជា​លខណ្ឌដែលមិនពិតដែលខុសពី​ if  មានន័យថាកាលណាវាត្រួតពិនិត្យលខណ្ឌរួចរាល់ហើយលខណ្ឌនោះពិតទើបវាបង្ហាញ ឬ អនុវត្ត ហើយទើបបង្ហាញទិន្ន័យ!!
// ភាពជោគជ័យផ្ដើមចេញពីកាព្យាយាម​ និង ខិតខំ!! លឺនៅក្មេងតូច

